# Infrastructure Notes

This folder contains infrastructure-oriented setup guidance and deployment templates.

Current milestone coverage:
- Render deployment via root `render.yaml`
- GitHub Actions CI and scheduled jobs via `.github/workflows/`
- Environment variable strategy via root `.env.example`
